const { createSlice } = require("@reduxjs/toolkit");

const initialState = {

}


export const TripDetailsSlice = createSlice({
    name: 'userAuth',
    initialState,
    reducers: {

    }
})

export const { } = TripDetailsSlice.actions

// selector


export default TripDetailsSlice.reducer

